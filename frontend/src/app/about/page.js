import { AboutPage } from "@/components/pages/about-page";

export const metadata = {
  title: "About",
  description:
    "Learn about the Face Morphing Detection project — the problem it solves, the clustering-based methodology, and the team behind it.",
};

export default function About() {
  return <AboutPage />;
}
