import { ContactPage } from "@/components/pages/contact-page";

export const metadata = {
  title: "Contact",
  description:
    "Get in touch with the FaceGuard team — bug reports, feature requests, partnerships, or general questions.",
};

export default function Contact() {
  return <ContactPage />;
}
