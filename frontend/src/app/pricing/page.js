import { PricingPage } from "@/components/pages/pricing-page";

export const metadata = {
  title: "Pricing",
  description:
    "Simple, transparent pricing for FaceGuard — free tier for demos, Pro for unlimited detections, and Enterprise for API access.",
};

export default function Pricing() {
  return <PricingPage />;
}
